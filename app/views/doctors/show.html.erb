<h2><%= @doctor.name %>'s Appointments:</h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
  <li>
    <%= link_to appointment.patient.name, patient_path(appointment.patient) %> at <%= appointment.readable_datetime %> in the <%= appointment.doctor.department %> Department
  </li>
  <% end %>
</ul>
