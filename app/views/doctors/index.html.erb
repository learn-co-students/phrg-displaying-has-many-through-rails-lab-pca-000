<h2>Doctors:</h2>
<ul>
  <% @doctors.each do |doctor| %>
  <li>
    <%= link_to doctor.name, doctor_path(doctor) %>, <%= doctor.department %> Department, Number of patients: <%= doctor.patients.size %>
  </li>
  <% end %>
</ul>
