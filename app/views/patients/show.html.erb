<% @patient.appointments.each do |appointment| %>
  <h3><%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></h3>
    <h3> <% @patient.doctors.each do |doctor| %></h3>
    <h3><%= link_to doctor.name, doctor_path(doctor) %></h3>
  <% end %>
<% end %>
