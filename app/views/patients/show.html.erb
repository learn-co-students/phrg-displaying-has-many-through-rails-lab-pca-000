<h1><%= @patient.name %></h1>
<ul>
  <% @patient.appointments.each do |apt| %>
    <li><%=apt.appointment_datetime.strftime("%B %d, %Y")%> at <%=apt.appointment_datetime.strftime("%H:%M")%> | Doctor: <%= link_to apt.doctor.name, doctor_path(apt.doctor) %></li>
  <% end %>
</ul>
