<h2><%= @patient.name %>'s Upcoming Appointments:</h2>
<ul>
  <% @patient.appointments.each do |appointment| %>
  <li>
    <%= appointment.readable_datetime %> with <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>
  </li>
  <% end %>
</ul>
